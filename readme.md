# Repo ejemplo de Cypress Cucumber

Este repo es de ejemplo usando cypress-cucumber y page object con un dockerfile

## Dependencias
- nodejs (version sin especificar)

## Instalacion
        npm install

## Ejecucion
        npm run cypress:run

# cypress-cucumber-docker
